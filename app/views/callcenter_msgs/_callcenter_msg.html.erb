<%= div_for callcenter_msg do %>
    <% if show_only %>    
       <%= Msg.where("id = ?", callcenter_msg.msg_id).select('code') %> 
    <% else %>
      <%= Msg.where("id = ?", callcenter_msg.msg_id).select('code') %> <%= link_to ' Del', callcenter_msg, method: :delete, :remote => true, :class => 'delete_callcenter_msg', data: { confirm: 'Are you sure?' } %>
    <% end %>
<% end %>