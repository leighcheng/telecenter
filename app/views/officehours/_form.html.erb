<%= form_for(@officehour) do |f| %>
  <% if @officehour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@officehour.errors.count, "error") %> prohibited this officehour from being saved:</h2>

      <ul>
      <% @officehour.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
