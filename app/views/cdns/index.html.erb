<h5>Listing CDN for </h5>

<table>
  <tr>
    <th>Extension</th>
    <th></th>
  </tr>

<% @cdns.each do |cdn| %>
  <tr>
    <td><%= cdn.extension %></td>
    <td><%= link_to 'Destroy', cdn, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

