<%= div_for cdn do %>
	<% if show_only %>
	    <%= cdn.extension %>
	<% else %>
        <%= cdn.extension %><%= link_to ' Del', cdn, method: :delete,:remote => true, :class => 'delete_cdn', data: { confirm: 'Are you sure?' } %>   
    <% end %>
<% end %>